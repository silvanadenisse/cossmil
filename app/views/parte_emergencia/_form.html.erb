<%= form_for(parte_emergencium) do |f| %>
  <% if parte_emergencium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(parte_emergencium.errors.count, "error") %> prohibited this parte_emergencium from being saved:</h2>

      <ul>
      <% parte_emergencium.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :mes %>
    <%= f.text_field :mes %>
  </div>

  <div class="field">
    <%= f.label :enfermera %>
    <%= f.text_field :enfermera %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
