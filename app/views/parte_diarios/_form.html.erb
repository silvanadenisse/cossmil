<%= form_for(parte_diario) do |f| %>
  <% if parte_diario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(parte_diario.errors.count, "error") %> prohibited this parte_diario from being saved:</h2>

      <ul>
      <% parte_diario.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label 'Mes: ' %>
    <%= f.text_field :mes %>
  </div>

  <div class="field">
    <%= f.label 'Servicio de: ' %>
    <%= f.text_field :servicio_de %>
  </div>

  <div class="field">
    <%= f.label 'Medico: ' %>
    <%= f.text_field :nombre_medico %>
  </div>

  <div class="field">
    <%= f.label 'clave: ' %>
    <%= f.number_field :clave %>
  </div>

  <div class="field">
    <%= f.label 'Inicio de consulta: ' %>
    <%= f.time_select :inicio_consulta %>
  </div>

  <div class="field">
    <%= f.label 'Fin de consulta: ' %>
    <%= f.time_select :fin_consulta %>
  </div>

  <div class="field">
    <%= f.label 'Enfermera: ' %>
    <%= f.text_field :enfermera %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
