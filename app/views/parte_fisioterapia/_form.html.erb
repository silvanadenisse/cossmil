<%= form_for(parte_fisioterapium) do |f| %>
  <% if parte_fisioterapium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(parte_fisioterapium.errors.count, "error") %> prohibited this parte_fisioterapium from being saved:</h2>

      <ul>
      <% parte_fisioterapium.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :turno %>
    <%= f.text_field :turno %>
  </div>

  <div class="field">
    <%= f.label :mes %>
    <%= f.text_field :mes %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
