<%= form_for(carga_emergencium) do |f| %>
  <% if carga_emergencium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(carga_emergencium.errors.count, "error") %> prohibited this carga_emergencium from being saved:</h2>

      <ul>
      <% carga_emergencium.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label 'Días habiles: ' %>
    <%= f.number_field :dias_habiles %>
  </div>

  <div class="field">
    <%= f.label 'Mes: ' %>
    <%= f.text_field :mes, value: l(Date.today, format: :short), readonly:true %>
  </div>

  <div class="field">
    <%= f.label 'Año: ' %>
    <%= f.number_field :anho, value: Date.today.year, readonly:true%>
  </div>

  <br><div class="actions">
    <%= f.submit "Crear reporte"%>
  </div></br>

<% end %>
