<%= form_for(parte_serviciocomplementario) do |f| %>
  <% if parte_serviciocomplementario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(parte_serviciocomplementario.errors.count, "error") %> prohibited this parte_serviciocomplementario from being saved:</h2>

      <ul>
      <% parte_serviciocomplementario.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
  <div class="field">
    <%= f.label 'Mes: ' %>
    <%= f.text_field :mes, value: l(Date.today, format: :short), readonly:true %>
  </div>

  </br><div class="actions">
      <%= f.submit "Crear parte diario" %>
  </div></br>
<% end %>
