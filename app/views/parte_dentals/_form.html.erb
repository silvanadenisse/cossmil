<%= form_for(parte_dental) do |f| %>
  <% if parte_dental.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(parte_dental.errors.count, "error") %> prohibited this parte_dental from being saved:</h2>

      <ul>
      <% parte_dental.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">

    <%= f.label 'Establecimiento: ' %>
    <%= current_user.establecimiento%>

    <%= f.label 'Localidad: ' %>
    <%= current_user.localidad%>

    <%= f.label 'Mes: ' %>
    <%= f.text_field :mes, value: l(Date.today, format: :short), readonly:true %>
  </div>

  <div class="field"> 
    <%= f.label 'Nombre del odontólogo: ' %>
    <%= current_user.name + " " +current_user.last_name%>
    <%= f.hidden_field :user_id, :value => current_user.id%>
  </div>

  <div class="field">
    <%= f.label 'Clave: ' %>
    <%= current_user.clave%>
  </div>

  <div class="field">
    <%= f.label 'Inicio de consulta: ' %>
    <%= f.time_select :inicio_consulta %>
  </div>

  <div class="field">
    <%= f.label 'Fin de consulta: ' %>
    <%= f.time_select :fin_consulta %>
  </div>

  <div class="field">
    <%= f.label 'Enfermera: ' %>
    <%= f.text_field :enfermera %>
  </div>

  </br><div class="actions">
      <%= f.submit "Crear parte diario" %>
    </div></br>
<% end %>
